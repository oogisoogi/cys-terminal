{
  "_doc": "T1-3 송신 ACL — role→role stdin 주입 정책. 데몬이 발신자 신원을 커널 peer pid로 직접 도출해 평가한다 (클라이언트 자기신고 from은 검증으로 대체). 규칙은 위에서부터 첫 매칭 승리, 글롭(*)은 역할명 패턴. from 특수값: 'external'=pane 밖 프로세스(오너 셸·Tauri 등), '(pane)'=역할 없는 pane. default: allow | deny.",
  "default": "allow",
  "rules": [
    { "from": "reviewer-*", "to": "worker*", "allow": false,
      "_why": "리뷰어는 워커 stdin을 직접 조향할 수 없다 — 중재는 master 경유" },
    { "from": "reviewer-*", "to": "master", "allow": true },
    { "from": "worker*", "to": "master", "allow": true }
  ]
}
